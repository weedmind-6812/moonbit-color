# moonbit-color 项目申报书

## 基本信息
- **项目名称**：moonbit-color - MoonBit 色彩处理库
- **GitHub 仓库链接**：https://github.com/weedmind-6812/moonbit-color
- **项目方向**：MoonBit 图形/可视化基础库
- **是否为移植项目**：否，原创项目（参考色彩理论和WCAG标准）

## 项目简介
moonbit-color 是一个全面的色彩处理库，为 MoonBit 生态提供专业的色彩操作能力。项目填补了 MoonBit 生态中缺少色彩处理工具的空白，为数据可视化、UI设计、图形渲染和生成艺术等领域提供基础支持。

**核心痛点**：目前 MoonBit 生态缺少专业的色彩处理库。开发者在做数据可视化、UI组件、图形应用时，需要手动实现色彩空间转换、渐变生成、对比度计算等常见功能。moonbit-color 提供开箱即用的解决方案，让开发者专注于业务逻辑。

**生态定位**：作为图形和可视化的基础设施库，moonbit-color 可以被其他高层库依赖（如图表库、UI框架、游戏引擎等），形成生态协同效应。

## 核心功能范围

### 1. 色彩空间支持与转换
- RGB 色彩空间（0.0-1.0浮点或0-255整数）
- HSL 色彩空间（色相/饱和度/明度）
- HSV 色彩空间（色相/饱和度/明度值）
- Hex 十六进制颜色字符串（#RRGGBB）
- 完整的双向转换能力（RGB ↔ HSL ↔ HSV ↔ Hex）

### 2. 色彩插值与渐变生成
- RGB 空间线性插值
- HSL 空间色相感知插值（处理色轮最短路径）
- 双色渐变生成（指定步数）
- 多点渐变生成（支持任意色标）

### 3. WCAG 可访问性支持
- 相对亮度计算（符合 WCAG 2.1 标准）
- 对比度计算（前景色与背景色）
- WCAG AA 合规检查（4.5:1，普通文本）
- WCAG AAA 合规检查（7:1，普通文本）
- 大文本对比度检查（3:1）

### 4. 调色板生成算法
- 互补色（色轮180°对角）
- 类似色（相邻30°，可指定数量）
- 三分色（120°等距）
- 四分色/正方形（90°等距）
- 单色调（相同色相，不同明度）
- 分裂互补色（150°/210°）

## 预期使用场景

### 场景1：数据可视化色阶生成
**问题**：图表库需要为数据生成连续或分类色阶。
**解决**：使用色彩插值和渐变生成功能，创建从浅到深的连续色阶，或使用调色板算法生成区分度高的分类色。

```moonbit
// 生成热力图色阶
let heatmap = @color.RGB::multi_gradient([
  @color.RGB::from_hex("#0000FF"),  // 蓝色（冷）
  @color.RGB::from_hex("#00FF00"),  // 绿色（中）
  @color.RGB::from_hex("#FF0000")   // 红色（热）
], 256)
```

### 场景2：UI主题系统
**问题**：UI框架需要根据主色生成完整的配色方案（按钮状态、边框、阴影等）。
**解决**：使用色彩空间转换调整明度/饱和度，使用调色板算法生成协调的辅助色。

```moonbit
let primary = @color.RGB::from_hex("#3B82F6")
let hover = @color.HSL::to_rgb({ h: hsl.h, s: hsl.s, l: hsl.l - 0.1 })  // 深10%
let complementary = primary.complementary()  // 强调色
```

### 场景3：可访问性合规检查
**问题**：设计系统需要确保文本颜色与背景颜色的对比度符合WCAG标准。
**解决**：使用对比度计算和合规检查函数，自动验证或建议替代配色。

```moonbit
if !@color.RGB::meets_wcag_aa(text_color, bg_color) {
  // 警告：对比度不足，影响可读性
  suggest_darker_text_color()
}
```

## 技术路径

### 实现路径
1. **色彩空间转换**：基于标准算法实现RGB/HSL/HSV相互转换，处理边界情况（灰度色、黑白等）
2. **插值算法**：RGB使用线性插值，HSL处理色轮环绕（避免跨越360°长路径）
3. **WCAG计算**：实现gamma校正和相对亮度公式，严格遵循W3C标准
4. **调色板生成**：基于色彩理论（色轮角度关系）和HSL操作实现各类和谐配色

### 技术理解
- **色彩空间选择**：RGB适合计算机存储，HSL/HSV适合人类直觉调整（如"让颜色变亮"直接调整L值）
- **WCAG标准**：不是简单的RGB差值，而是基于人眼感知的亮度加权计算
- **调色板和谐性**：基于色相角度关系（互补、类似、三分等）能产生视觉协调的配色

### 所需能力
- 色彩理论基础（色轮、HSL/HSV模型）
- 数学运算（三角函数、平方根、幂运算）
- 算法实现（牛顿迭代、边界条件处理）
- 工程实践（API设计、测试覆盖、文档编写）

## 交付范围
- ✅ 完整的色彩空间转换功能（RGB/HSL/HSV/Hex）
- ✅ 色彩插值与渐变生成（单色、双色、多色）
- ✅ WCAG 2.1 对比度计算和合规检查
- ✅ 6种调色板生成算法
- ✅ 18个单元测试（覆盖所有核心功能）
- ✅ 完整的API文档和使用示例
- ✅ GitHub Actions CI/CD（check/test/build）
- ✅ 发布到 mooncakes.io

## 明确不做的内容
- 不实现LAB/LCH色彩空间（需要更复杂的D65白点转换）
- 不实现图像文件读写（属于图像处理库范畴）
- 不提供UI组件（仅提供底层色彩计算能力）
- 不实现色盲模拟（超出当前范围，可作为未来扩展）

## 参考资料与开源规范
- **W3C WCAG 2.1 Contrast Guidelines**：对比度计算的权威标准
- **Wikipedia - HSL and HSV**：色彩空间转换算法数学公式
- **Color Harmony Theory**：调色板生成的色彩理论基础

**参考项目**（仅参考思路，算法独立实现）：
- chroma.js（BSD-3-Clause）
- colord（MIT）

所有代码从零实现，不复制任何现有项目代码。仅参考数学公式和色彩理论原理。

## 项目许可证
Apache License 2.0 - 允许商业使用、修改和分发，要求保留版权声明。

---

**项目特色**：
1. **完整性**：覆盖色彩处理的核心需求（转换、插值、对比度、调色板）
2. **可用性**：API设计简洁，开箱即用，适合新手和专家
3. **合规性**：严格遵循WCAG标准，支持可访问性开发
4. **协同性**：可与其他生态项目（如moonforce力导向图）结合使用
