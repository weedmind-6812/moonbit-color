# moonbit-color 项目申报书

## 基本信息

- **项目名称**：moonbit-color - MoonBit 色彩处理库
- **参赛者**：黄渲棋
- **联系方式**：[待填写]
- **GitHub 仓库链接**：https://github.com/weedmind-6812/moonbit-color
- **Gitlink 仓库链接**：[待填写]
- **项目方向**：图形/可视化基础库
- **是否为移植项目**：否，原创项目

---

## 项目简介

moonbit-color 是一个全面的色彩处理库，为 MoonBit 生态提供专业的色彩操作能力。项目填补了 MoonBit 生态中缺少色彩处理工具的空白，为数据可视化、UI设计、图形渲染和生成艺术等领域提供基础支持。

**核心痛点**：目前 MoonBit 生态缺少专业的色彩处理库。开发者在做数据可视化、UI组件、图形应用时，需要手动实现色彩空间转换、渐变生成、对比度计算等常见功能，重复造轮子且容易出错。moonbit-color 提供开箱即用的解决方案，让开发者专注于业务逻辑。

**生态定位**：作为图形和可视化的基础设施库，moonbit-color 可以被其他高层库依赖（如图表库、UI框架、游戏引擎等），形成生态协同效应。与 moonbit-easing 配合可实现流畅的色彩动画效果。

---

## 项目方向与适用场景

### 应用场景

1. **数据可视化**
   - 为图表生成连续色阶（热力图、等高线图）
   - 为分类数据生成区分度高的离散配色
   - 动态调整数据映射的颜色方案

2. **UI 设计与主题系统**
   - 根据主色自动生成完整配色方案
   - 实现深色/浅色主题切换
   - 生成按钮 hover/active 状态颜色

3. **可访问性开发**
   - 确保文本颜色符合 WCAG 对比度标准
   - 自动建议符合无障碍要求的替代色
   - 辅助设计系统通过 AA/AAA 合规测试

4. **图形渲染与生成艺术**
   - 程序化生成协调的配色方案
   - 创建自然的色彩过渡效果
   - 实现基于色彩理论的艺术创作

### 技术价值

- **填补生态空白**：MoonBit 首个专业色彩处理库
- **标准化**：遵循 W3C WCAG 2.1 标准，确保可访问性
- **可组合**：色彩空间自由转换，算法灵活组合
- **零依赖**：纯 MoonBit 实现，无外部依赖

---

## 拟实现的核心功能

### 1. 色彩空间支持与转换

| 色彩空间 | 描述 | 用途 |
|---------|------|------|
| **RGB** | Red, Green, Blue (0.0-1.0 或 0-255) | 计算机存储和显示标准 |
| **HSL** | Hue, Saturation, Lightness | 人类直觉的颜色调整 |
| **HSV** | Hue, Saturation, Value | 图形设计和选色器 |
| **Hex** | 十六进制 (#RRGGBB) | Web 开发标准格式 |

**完整的双向转换**：RGB ↔ HSL ↔ HSV ↔ Hex

**实现要点**：
- 处理边界情况（灰度色、纯黑、纯白）
- HSL/HSV 的色相环绕（0° = 360°）
- Hex 解析容错（支持 #RGB 短格式）

### 2. 色彩插值与渐变生成

**线性插值**：
- RGB 空间：逐通道线性混合
- HSL 空间：色相感知插值（处理色轮最短路径）

**渐变生成**：
- 双色渐变：指定起始色、结束色和步数
- 多点渐变：支持任意色标（3色、5色、N色）
- 自动色标分布：均匀或自定义位置

**应用示例**：
```moonbit
// 热力图色阶：蓝 → 绿 → 红
let heatmap = @color.RGB::multi_gradient([
  @color.RGB::from_hex("#0000FF"),  // 冷
  @color.RGB::from_hex("#00FF00"),  // 中
  @color.RGB::from_hex("#FF0000")   // 热
], 256)
```

### 3. WCAG 可访问性支持

**WCAG 2.1 对比度标准**：
- **AA 级（普通文本）**：4.5:1
- **AA 级（大文本）**：3:1
- **AAA 级（普通文本）**：7:1
- **AAA 级（大文本）**：4.5:1

**核心能力**：
- 相对亮度计算（gamma 校正）
- 对比度计算（前景色 vs 背景色）
- 合规性检查（自动判断是否通过标准）

**实现细节**：
- 严格遵循 W3C 相对亮度公式
- sRGB gamma 校正（非线性 → 线性）
- 对比度公式：`(L1 + 0.05) / (L2 + 0.05)`

### 4. 调色板生成算法

基于色彩和谐理论，提供 6 种专业配色方案：

| 算法 | 色相关系 | 应用场景 |
|------|---------|----------|
| **互补色** | 180° 对角 | 强烈对比，强调重点 |
| **类似色** | 相邻 ±30° | 和谐统一，品牌色系 |
| **三分色** | 120° 等距 | 均衡活泼，三类数据 |
| **四分色** | 90° 等距 | 丰富多彩，四类数据 |
| **单色调** | 同色相不同明度 | 层次感，深浅变化 |
| **分裂互补** | 150°/210° | 对比但不刺眼 |

**技术实现**：
- 在 HSL 空间旋转色相角度
- 保持饱和度和明度一致（或渐变）
- 处理色轮环绕（超过 360° 回到 0°）

---

## 参考资料与开源规范

### 标准与理论依据

- **W3C WCAG 2.1 Contrast Guidelines** - 对比度计算权威标准
  - https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum.html
- **Wikipedia - HSL and HSV** - 色彩空间转换算法
  - https://en.wikipedia.org/wiki/HSL_and_HSV
- **Color Harmony Theory** - 调色板生成理论基础
  - https://www.tigercolor.com/color-lab/color-theory/color-harmonies.htm

### 参考项目（思路参考）

- **chroma.js** (BSD-3-Clause) - JavaScript 色彩处理库
  - https://gka.github.io/chroma.js/
- **colord** (MIT) - 轻量级色彩工具
  - https://github.com/omgovich/colord

**声明**：所有代码从零实现，仅参考数学公式和色彩理论原理，不复制任何现有项目代码。算法基于公开的标准规范（W3C、Wikipedia）独立编写。

### 本项目许可证

**Apache License 2.0** - 允许商业使用、修改和分发，要求保留版权声明。

---

## 计划开发内容

### ✅ 已完成内容

#### 核心功能实现
- ✅ RGB/HSL/HSV/Hex 四种色彩空间
- ✅ 完整的双向转换能力
- ✅ RGB 和 HSL 插值算法
- ✅ 双色和多色渐变生成
- ✅ WCAG 2.1 相对亮度计算
- ✅ 对比度计算和合规检查（AA/AAA）
- ✅ 6 种调色板生成算法

#### 测试覆盖
- ✅ 18 个单元测试
- ✅ 色彩空间转换测试
- ✅ Hex 解析和格式化测试
- ✅ 插值和渐变测试
- ✅ 亮度和对比度测试
- ✅ 调色板生成测试
- ✅ 边界条件测试

#### 工程化
- ✅ GitHub Actions CI/CD（check/test/build）
- ✅ 完整的 README.md 文档（中英双语）
- ✅ API 参考和使用示例
- ✅ 项目申报书（PROPOSAL.md）
- ✅ LICENSE（Apache 2.0）
- ✅ .gitignore 和 moon.mod.json 配置

#### 示例程序
- ✅ examples/basic/ 基础使用示例
- ✅ 覆盖所有核心 API

---

## 技术路径

### 1. 色彩空间转换实现

**RGB → HSL**：
- 找出 RGB 的最大值和最小值
- 计算明度 L = (max + min) / 2
- 计算饱和度 S（根据 L 的不同范围使用不同公式）
- 计算色相 H（根据最大通道是 R/G/B 使用不同公式）
- 处理灰度色（S=0 时 H 未定义）

**HSL → RGB**：
- 根据 H, S, L 计算色度 C 和亮度偏移 m
- 计算 H 在色轮的 60° 分段位置
- 根据分段计算 R', G', B'
- 添加亮度偏移得到最终 RGB

**RGB ↔ HSV** 和 **Hex** 转换类似。

### 2. 插值与渐变算法

**RGB 插值**：
```moonbit
r = r1 * (1 - t) + r2 * t
g = g1 * (1 - t) + g2 * t
b = b1 * (1 - t) + b2 * t
```

**HSL 插值**（色相环绕处理）：
```moonbit
// 找最短路径
let diff = h2 - h1
if diff > 180.0 {
  h2 = h2 - 360.0
} else if diff < -180.0 {
  h1 = h1 - 360.0
}
h = h1 * (1 - t) + h2 * t
```

**多色渐变**：
- 将 [0, 1] 区间分段
- 每段内使用双色插值
- 自动计算色标位置

### 3. WCAG 对比度计现

**相对亮度**（sRGB gamma 校正）：
```moonbit
fn srgb_to_linear(c: Double) -> Double {
  if c <= 0.03928 {
    c / 12.92
  } else {
    pow((c + 0.055) / 1.055, 2.4)
  }
}

L = 0.2126 * R_linear + 0.7152 * G_linear + 0.0722 * B_linear
```

**对比度**：
```moonbit
ratio = (L_lighter + 0.05) / (L_darker + 0.05)
```

### 4. 调色板生成

**基于 HSL 的色相旋转**：
```moonbit
fn rotate_hue(base: HSL, angle: Double) -> HSL {
  let new_h = (base.h + angle) % 360.0
  { h: new_h, s: base.s, l: base.l }
}

// 互补色
complementary = rotate_hue(base, 180.0)

// 类似色
analogous = [
  rotate_hue(base, -30.0),
  base,
  rotate_hue(base, 30.0)
]
```

---

## 预期交付成果

### 1. 核心库 ✅
- **moonbit-color** 包
- 零外部依赖（除 moonbitlang/core）
- 代码规模：约 800 行核心代码 + 400 行测试

### 2. 测试套件 ✅
- 18 个单元测试
- 覆盖所有公开 API
- CI 自动运行，100% 通过

### 3. 文档 ✅
- **README.md** - 项目介绍、快速开始、API 参考、示例（中英双语）
- **PROPOSAL.md** - 项目申报书
- 代码内联注释

### 4. 示例程序 ✅
- examples/basic/ - 基础使用示例
- 覆盖色彩转换、插值、对比度、调色板

### 5. 工程配置 ✅
- GitHub Actions CI
- .gitignore / LICENSE / moon.mod.json
- 代码格式化规范（moon fmt）

### 6. 发布 📅
- 发布到 mooncakes.io（验收前完成）

---

## 项目现有基础

截至报名提交时，本项目已完成：

1. **核心功能 100%**
   - 4 种色彩空间完整实现
   - 所有转换、插值、对比度、调色板算法
   - API 设计清晰，类型安全

2. **测试覆盖充分**
   - 18 个单元测试全部通过
   - 边界条件和异常情况覆盖

3. **文档完整**
   - 中英双语 README
   - API 参考和使用示例
   - 项目申报书

4. **工程质量**
   - GitHub Actions CI 配置
   - 代码格式化和类型检查通过
   - 项目结构清晰

5. **开源合规**
   - Apache 2.0 许可证
   - 代码独立实现，遵守参考资料许可

---

## 项目价值与创新点

### 生态价值
- **填补空白**：MoonBit 首个专业色彩处理库
- **基础设施**：为数据可视化、UI 框架等提供底层支持
- **标准化**：统一的色彩操作 API，降低学习成本

### 技术亮点
- **类型安全**：利用 MoonBit 类型系统确保正确性
- **算法完整**：涵盖色彩处理的核心需求
- **符合标准**：严格遵循 W3C WCAG 2.1 标准
- **零依赖**：纯 MoonBit 实现，易于集成

### 可扩展性
- **色彩空间可扩展**：未来可添加 LAB/LCH
- **算法可组合**：插值、渐变、调色板可灵活组合
- **平台无关**：纯计算库，可编译为任何后端

### 协同效应
与 **moonbit-easing** 结合实现流畅色彩动画：
```moonbit
// 红到蓝的缓动渐变
let frames = @easing.ease_sequence(0.0, 1.0, 60, @easing.ease_out_cubic)
for t in frames {
  let color = @color.RGB::lerp(red, blue, t)
  render(color)
}
```

---

## 工作量统计

- **核心代码**：~800 行 MoonBit（color.mbt + interpolation.mbt + contrast.mbt + palette.mbt）
- **测试代码**：~400 行 MoonBit（color_test.mbt）
- **文档**：~600 行 Markdown（README.md + PROPOSAL.md + 申报书）
- **配置文件**：GitHub Actions、moon.mod.json、.gitignore 等

**总计**：约 1,800 行有效代码与文档，符合比赛 4,000~10,000 行参考区间的入门级项目。

---

## 后续维护计划

1. **功能扩展**
   - 添加 LAB/LCH 色彩空间（感知均匀性）
   - 色盲模拟功能
   - 更多调色板算法（双补色、方形等）

2. **性能优化**
   - 缓存常用转换结果
   - 批量操作优化

3. **生态集成**
   - 与 MoonBit 图表库协作
   - 为 UI 框架提供主题生成工具

4. **文档完善**
   - 撰写教程和最佳实践
   - 添加更多示例程序

---

## 联系方式

- **参赛者**：黄渲棋
- **GitHub**：https://github.com/weedmind-6812
- **项目仓库**：https://github.com/weedmind-6812/moonbit-color
- **邮箱**：[待填写]
- **微信**：[待填写]

---

**声明**：本项目所有代码从零独立实现，仅参考公开的数学公式和色彩理论标准（W3C WCAG、Wikipedia）。不包含任何第三方项目的代码拷贝。
